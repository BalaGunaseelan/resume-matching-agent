# resume-matching-agent
Learning ground for HVE-Core based development

## Repository ruleset automation

The main branch ruleset definition lives at `/home/runner/work/resume-matching-agent/resume-matching-agent/.github/rulesets/main-branch-protection.yml`.

Apply it with the `/home/runner/work/resume-matching-agent/resume-matching-agent/.github/workflows/apply-main-branch-ruleset.yml` workflow after adding a `RULESET_ADMIN_TOKEN` repository secret with repository administration access.
